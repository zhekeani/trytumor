export enum DeploymentStage {
  Development = 'development',
  Testing = 'testing',
  Staging = 'staging',
  Production = 'production',
}

export enum EnvironmentRuntime {
  Local = 'local',
  Remote = 'remote',
}
